<Page
    x:Class="Elementary.BiblePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Elementary"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:viewmodels="using:Elementary.ViewModels"
    xmlns:common="using:html2xaml"
    mc:Ignorable="d"
    d:DataContext="{d:DesignInstance viewmodels:BiblePageViewModel, IsDesignTimeCreatable=True}"
    Background="Transparent"
    Tag="BiblePage">

    <Grid Width="Auto">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center" Spacing="8" Margin="8,8,8,8">
            <ComboBox x:Name="BibleBookComboBox" ItemsSource="{x:Bind _viewModel.Bible.Books}" SelectedItem="{x:Bind _viewModel.CurrentBook.Title}" SelectionChanged="BibleBookComboBox_SelectionChanged" PlaceholderText="Book"/>
            <ComboBox x:Name="BookChapterComboBox" ItemsSource="{x:Bind _viewModel.ChapterIndices}" SelectedItem="{x:Bind _viewModel.SelectedChapterIndex}" SelectionChanged="BookChapterComboBox_SelectionChanged" PlaceholderText="Chapter"/>
        </StackPanel>
        <ScrollViewer x:Name="BibleScrollViewer" Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <Grid x:Name="RichTextGrid" SizeChanged="WebViewGrid_SizeChanged">
                <RichTextBlock x:Name="ChapterView" common:Properties.Html="{Binding CurrentChapter.ChapterText}" FontFamily="{Binding Font}" FontSize="{Binding FontSize}" />
            </Grid>
        </ScrollViewer>
    </Grid>
</Page>