<Page
    x:Class="Elementary.BiblePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Elementary"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:viewmodels="using:Elementary.ViewModels"
    xmlns:common="using:html2xaml"
    mc:Ignorable="d"
    d:DataContext="{d:DesignInstance viewmodels:BiblePageViewModel, IsDesignTimeCreatable=True}"
    Background="Transparent"
    Tag="BiblePage">

    <Grid Width="Auto">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="15*"/>
            <ColumnDefinition Width="70*"/>
            <ColumnDefinition Width="15*"/>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Row="0" Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Center" Spacing="8" Margin="8,8,8,8">
            <ComboBox x:Name="BibleBookComboBox" ItemsSource="{x:Bind _viewModel.Bible.Books}" SelectedItem="{x:Bind _viewModel.Book}" DisplayMemberPath="Title" SelectionChanged="BibleBookComboBox_SelectionChanged" PlaceholderText="Book"/>
            <ComboBox x:Name="BookChapterComboBox" ItemsSource="{x:Bind _viewModel.Book.Chapters}" SelectedItem="{x:Bind _viewModel.Chapter}" DisplayMemberPath="Index" SelectionChanged="BookChapterComboBox_SelectionChanged" PlaceholderText="Chapter"/>
        </StackPanel>
        <Button Grid.Row="1" Grid.Column="0" Content="&lt;" HorizontalAlignment="Center" VerticalAlignment="Center" Height="50" Click="BackButton_Click"/>
        <ScrollViewer Grid.Row="1" Grid.Column="1" VerticalScrollBarVisibility="Auto">
            <Grid x:Name="RichTextGrid" SizeChanged="WebViewGrid_SizeChanged">
                <RichTextBlock x:Name="ChapterView" common:Properties.Html="{Binding CurrentChapterContent}" FontFamily="SegoeUI" FontSize="18" />
            </Grid>
        </ScrollViewer>
        <Button Grid.Row="1" Grid.Column="2" Content="&gt;" HorizontalAlignment="Center" VerticalAlignment="Center" Height="50" Click="ForwardButton_Click"/>
    </Grid>
</Page>
