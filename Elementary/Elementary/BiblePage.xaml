<Page
    x:Class="Elementary.BiblePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Elementary"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:viewmodels="using:Elementary.ViewModels"
    mc:Ignorable="d"
    d:DataContext="{d:DesignInstance viewmodels:BiblePageViewModel, IsDesignTimeCreatable=True}"
    Background="Transparent"
    Tag="BiblePage">

    <Grid MaxWidth="700" MinWidth="300" Margin="16,0,16,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition />
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Grid.Row="0" HorizontalAlignment="Center" Spacing="8" Margin="8,8,8,8">
            <ComboBox x:Name="BibleBookComboBox" ItemsSource="{x:Bind _viewModel.Bible.Books}" DisplayMemberPath="Title" PlaceholderText="Book"/>
            <ComboBox x:Name="BookChapterComboBox" ItemsSource="{x:Bind _viewModel.CurrentBookChapters}" PlaceholderText="Chapter"/>
        </StackPanel>
        <ScrollViewer Grid.Row="1">
            <WebView x:Name="ChapterView" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" DefaultBackgroundColor="Transparent"/>
        </ScrollViewer>
    </Grid>
</Page>
